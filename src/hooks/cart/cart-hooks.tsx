import { statetype, storetype } from "../../types/typesforapp";

const state:statetype = [];

const additem = () =>{
    return "ADDITEM";
}

const removeitem = () =>{
    return "REMOVEITEM"
}


const store = (prop:storetype) =>{
    switch(prop.event.)
}